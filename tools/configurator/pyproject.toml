[project]
name = "configurator"
version = "0.1.0"
description = "define areas in an app"
readme = "README.md"
authors = [{name="Sony Semiconductor Solutions Corp"}]
requires-python = ">=3.11,<3.14"
dependencies = [
    "modlib==1.3.1",
]

[project.scripts]
configurator = "configurator.example:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
