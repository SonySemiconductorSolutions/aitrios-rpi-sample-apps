[project]
name = "nearest-person"
version = "1.0.0"
description = "Nearest person application detects all people and calculates the real world distance of the next nearest person. If under 2 meters add to list"
authors = [{name="Sony Semiconductor Solutions Corp"}]
readme = "README.md"
license = "Apache-2.0"
requires-python = ">=3.11,<3.14"

dependencies = ["modlib==1.3.1"]

